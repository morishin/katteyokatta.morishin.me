query searchAmazonItems($searchArgs: AmazonItemsSearchArgs!) {
  amazonItems(searchArgs: $searchArgs) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        asin
        name
        image
        amazonUrl
        price
      }
    }
  }
}
