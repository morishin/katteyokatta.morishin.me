query getPostsByUser($userName: String!, $page: PageArgs!) {
  posts(page: $page, userName: $userName) {
    pageInfo {
      hasPreviousPage
      startCursor
    }
    edges {
      node {
        ...DefaultPost
      }
    }
  }
}
