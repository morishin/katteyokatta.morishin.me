query getPostsByUserId($userId: Int, $page: PageArgs!) {
  posts(page: $page, userId: $userId) {
    pageInfo {
      hasPreviousPage
      startCursor
    }
    edges {
      node {
        ...DefaultPost
      }
    }
  }
}
